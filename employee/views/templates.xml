<odoo>
    <data>
       <!--  <record model='ir.ui.view' id="student_list">
            <field name="name">Student list</field>
            <field name="model">student.register</field>
             <field name="arch" type="xml">
            <tree>
          <field name="name"/>
          <field name="email"/>
          <field name="contact"/>
          <field name="qualification"/>
        </tree>
      </field>
    </record>


    <record  model='ir.ui.view' id="student_form">
      <field name="name">Student Form</field>
      <field name="model">student.register</field>
      <field name="arch" type="xml"/>
        <form string="Student registartion">
          <sheet>

          <group>
            <group>
              <filed name="name"/>
              <filed name="age"/>

            </group>
            <group>
            <filed name="date_of_birth"/>
            <field name="gender"/>
         </group>   
          </group>
           </sheet>
          <notebook>
            <page name="public" string="Contact-Information">
              <group>
                <group>
              <filed name="address"/>
              <filed name="email"/>
              <filed name="contact"/>
              </group>
            </group>
            </page>
            <page name="public" string="EducationalDetails">
              <group >
                <group>
                  <filed name="qualification"/>
                </group>
              </group>

            </page>
            <page name="public" string="UserDeatsils">
              <group>
                <group>
                  <filed name="username"/>
                  <filed name="password"/>
                </group>
              </group>
            </page>

          </notebook >
       
        </form>
    </record>
    <record model="ir.actions.act_window" id="student_form_action">
      <field name="name">Student window</field>
      <field name="res_model">student.register</field>
      <field name="view_model">tree,form</field>
    </record>

    <menuitem name="Instistute" id="instistute_menu"/>
    <menuitem name="Instistute-list" id="instistute.menu_1" parent="instistute_menu" action="student_form_action"/>
 -->
    </data>
</odoo>